// Kept for backward compatibility — ThemeReveal is the canonical component.
export { ThemeReveal as ShadowThemeUnlock, ThemeReveal } from './ThemeReveal';
export type { ThemeRevealProps } from './ThemeReveal';
export default ThemeReveal;

import ThemeReveal from './ThemeReveal';
